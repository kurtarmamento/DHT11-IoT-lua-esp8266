# PC-side configuration (for Node-RED / local tooling / future docker-compose)
DEVICE_ID=room-sensor-01

MQTT_HOST=127.0.0.1
MQTT_PORT=1883

# Treat as secret; do NOT commit real webhook URLs
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/REPLACE_ME
